import {PERFORMANCE_PREFIX} from "../shared/consts";

const GET_AUCTION_COUNT_MAX_FEE = 0;
const GET_AUCTION_BY_INDEX_MAX_FEE = 0;
const LIST_NFT_FOR_AUCTION_MAX_FEE = 0;
const START_AUCTION_MAX_FEE = 0;
const CANCEL_AUCTION_MAX_FEE = 0;
const BID_NFT_MAX_FEE = 0;
const CLAIM_NFT_MAX_FEE = 0;

describe(PERFORMANCE_PREFIX + 'Auction', function() {
    it('Should get auction count within max fee', async function() {
        // Test logic for get_auction_count
        // Make sure that the fee does not exceed GET_AUCTION_COUNT_MAX_FEE
    });

    it('Should get auction by index within max fee', async function() {
        // Test logic for get_auction_by_index
        // Make sure that the fee does not exceed GET_AUCTION_BY_INDEX_MAX_FEE
    });

    it('Should list NFT for auction within max fee', async function() {
        // Test logic for list_nft_for_auction
        // Make sure that the fee does not exceed LIST_NFT_FOR_AUCTION_MAX_FEE
    });

    it('Should start auction within max fee', async function() {
        // Test logic for start_auction
        // Make sure that the fee does not exceed START_AUCTION_MAX_FEE
    });

    it('Should cancel auction within max fee', async function() {
        // Test logic for cancel_auction
        // Make sure that the fee does not exceed CANCEL_AUCTION_MAX_FEE
    });

    it('Should bid on NFT within max fee', async function() {
        // Test logic for bid_nft
        // Make sure that the fee does not exceed BID_NFT_MAX_FEE
    });

    it('Should claim NFT within max fee', async function() {
        // Test logic for claim_nft
        // Make sure that the fee does not exceed CLAIM_NFT_MAX_FEE
    });
});
